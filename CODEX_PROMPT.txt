Build the `bespokesynth_mcp` platform from `plan.md` using Python 3.11+ and the official MCP Python SDK. Implement a FastMCP server with stdio and Streamable HTTP transports, safe/admin BespokeSynth OSC tools, idempotency, dry-run, auth/origin checks, and telemetry capture. Add a Bespoke script-agent example using `me.connect_osc_input`, `on_osc`, `me.set`, `me.get`, and `oscoutput`. Add a FastAPI orchestrator with plan/execute/chat endpoints, policy modes, consent gates, and provider routing for OpenAI, Anthropic, Gemini, Ollama, and llama.cpp. Ship tests, Docker, CI, docs, and sample CLI configs.

